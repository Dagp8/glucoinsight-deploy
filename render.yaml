services:
  - type: web
    name: glucoinsight-web
    env: node
    buildCommand: npm ci && npm run build:css
    startCommand: bash start.sh
    envVars:
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false
      - key: MONGO_URI
        sync: false
