<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body class="bg-gray-100 text-gray-800 font-sans">
    <!-- Header -->
    <%- include("partials/header") %>

    <main
      class="min-h-screen px-4 py-10 flex flex-col items-center justify-start"
    >
      <!-- Results & Image side by side -->
      <div
        class="w-full max-w-6xl grid grid-cols-1 md:grid-cols-2 gap-10 items-center mb-12"
      >
        <!-- Image Section -->
        <div class="flex items-center justify-center">
          <img
            src="/img/imgh1.png"
            alt="Result Illustration"
            class="max-w-full w-80 h-auto object-contain"
          />
        </div>

        <!-- Result Section -->
        <div class="bg-white p-8 rounded-2xl shadow-md text-center">
          <h2 class="text-3xl font-bold text-blue-600 mb-4">
            Diabetes Risk Estimation
          </h2>
          <p class="text-lg mb-2">Your estimated risk is:</p>
          <p class="text-4xl font-bold text-<%= color %>-600 mb-4">
            <%= risk %>%
          </p>
          <p class="text-lg font-medium mb-2">Risk level: <%= level %></p>
          <p class="text-sm text-gray-600 mb-6"><%= message %></p>
          <a
            href="/form"
            class="inline-block bg-blue-600 text-white px-5 py-2 rounded hover:bg-blue-700"
            >Try Again</a
          >
        </div>
      </div>

      <!-- Recommendations Section -->
      <div class="w-full max-w-6xl">
        <h3 class="text-xl font-semibold text-blue-700 mb-2">
          Recommended Resources
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <%- include("partials/resources", { level: level }) %>
        </div>
      </div>

      <!-- Rating section -->
      <div class="max-w-xl mt-10 bg-white p-6 rounded-xl shadow text-center">
        <h3 class="text-lg font-semibold mb-3 text-gray-800">
          How would you rate your experience?
        </h3>

        <form action="/rate" method="POST" id="ratingForm">
          <div class="flex justify-center gap-2 text-2xl mb-4">
            <% for (let i = 1; i <= 5; i++) { %>
            <button
              type="submit"
              name="stars"
              value="<%= i %>"
              class="text-gray-400 hover:text-yellow-500 transition"
            >
              ★
            </button>
            <% } %>
          </div>
        </form>

        <p class="text-sm text-gray-500">
          Your feedback helps us improve GlucoInsight.
        </p>
      </div>
    </main>

    <!-- Footer -->
    <%- include("partials/footer") %>

    <div class="hidden">
      text-red-600 text-orange-600 text-yellow-600 text-green-600 text-teal-600
    </div>
  </body>
</html>
